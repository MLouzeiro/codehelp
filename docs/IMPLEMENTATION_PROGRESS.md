# IMPLEMENTATION_PROGRESS — CodeHelp CRM/Helpdesk

> Este arquivo é o **registro canônico de progresso**. Atualizar a cada fase concluída.
> Criado em: 2026-08-13 (FASE 0 — Diagnóstico).

Legenda status: ⏳ pendente · 🔧 em andamento · ✅ concluído · ⛔ bloqueado · 🔄 revisado

## Fases (ordem de execução recomendada)

| # | Fase | Prioridade | Status |
|---|------|-----------|--------|
| 0 | Diagnóstico do sistema + documentação base (ARCHITECTURE/FLOWS/PROGRESS) | Alta | ✅ |
| 1 | Suíte de testes integrada (infra de testes padrão + smoke dos fluxos críticos) | Alta | ✅ |
| 2 | Fluxos críticos de negócio (WhatsApp → ticket → atendimento → encerramento → avaliação) | Alta | ✅ |
| 3 | Time Tracking (apontamento de horas, relatórios, vínculo com OS/ticket) | Alta | ✅ |
| 4 | Aprovações (fluxo completo: solicitação → notificação → decisão → histórico) | Média | ✅ |
| 5 | Relatórios gerenciais (semanais, por analista, FCR, SLA, CSAT) | Média | ✅ |
| 6 | Dashboard de indicadores (painel gerencial consolidado) | Média | ✅ |
| 7 | Auditoria IA (agente de monitoramento de conversas) | Média | ✅ |
| 8 | Auditoria individual por analista (relatório + replay de conversa) | Média | ✅ |
| 9 | Refatoração de código legado (duplicações, handler legado) | Baixa | ✅ |
| 10 | Documentação final (arquivos já criados + atualização AGENTS.md) | Média | ✅ |
| 11 | Testes finais integrados (regressão completa) | Alta | ✅ |
| 12 | Ajustes finais de fluxo, relatórios e auditoria | Alta | 🔧 |

## Gestão Unificada (componentes globais de relatórios)

- [x] **Backend — Excel**: `exceljs ^4.4.0` instalado; `gerarExcelRelatorio` + helpers `estiloCabecalho`/`adicionarSecao` em `relatorios.service.ts`; `getRelatorioExcel` no controller; rota `GET /api/analytics/relatorios/excel` (admin/gerente).
- [x] **Backend — Auditoria filtros + Excel**: `FiltroAuditoria` estendido (`departamentoId`, `categoria`, `prioridade`, `status`) aplicado em `listarTicketsEncerrados`/lote/resumo; `parseFiltro` do closure-audit estendido; `exportarAuditoriaExcel` (resumo + detalhe) em `closureAudit.service.ts`; `getExportarAuditoriaExcel` + rota `GET /helpdesk/closure-audit/exportar-excel` (admin/gerente).
- [x] **Frontend — componentes globais** `src/components/reports/`: `ReportFilters` (período Hoje/7/14/30/90 + datas personalizadas, filtros fila/canal/prioridade/status/departamento/analista/cliente/categoria/assunto, contador de filtros ativos + "Limpar filtros"), `ReportKpiCard`, `DeltaPill`, `ReportActions` (Atualizar/CSV/Excel/PDF/Imprimir). CSS `@media print` no `index.css` (oculta aside/header, quebra de página).
- [x] **Aplicação do padrão**: `AuditoriaEncerramentoPage` (filtros completos ponta a ponta + Excel/print via `ReportActions`), `AuditoriaAnalistaPage` (KPI cards + print), `RelatorioAnalitico` (botão Excel), `TimeTrackingPage` (botão print).
- [x] Testes novos: Excel relatório (buffer PK) + filtros auditoria (prioridade/categoria) + Excel auditoria. Backend **377 pass / 1 fail pré-existente time-dependent** (TESTE #31 expediente — falha só quando executa fora do horário real, ex. sábado 21h; não relacionado), tsc backend 20 pré-existentes (0 novos), frontend tsc 0, frontend 5/5.

## Histórico de mudanças

| Data | Fase | Descrição | Resultado |
|------|------|-----------|-----------|
| 2026-08-15 | 12 | **FASE 12 — Fase F: Segurança, performance, fonte única e regressão completa** (item #17/#18). **Segurança/validação**: `parseFiltro` (closure-audit) agora rejeita `dataInicio`/`dataFim` inválidas (throw) e limita `limit` a 1..1000 (export usa default 500); exportado para teste. **Fonte única de dados**: resumo usa constantes `WHERE_TICKET_RESOLVIDO` + `MOTIVO_ENCERRADO_SEM_RESOLUCAO`; risco calculado centralmente em `auditarEncerramento` (sem lógica duplicada no resumo). **Performance**: `problemaNaoResolvido` via `prisma.ticket.count` no banco (agregação, sem carregar tickets); demais indicadores agregados em memória a partir do lote limitado. TESTE #34 (`closure-audit-validation.test.ts`, 6 testes): datas inválidas, limite fora do range (0/1001/abc), limites 1/1000 aceitos, filtros válidos, campos ausentes → undefined. | Backend **375/375** (35 arquivos; 14 em auditoria), tsc backend 20 pré-existentes (0 novos), frontend tsc 0, frontend 5/5. |
| 2026-08-15 | 12 | **FASE 12 — Fase E: Auditoria de encerramento evoluída** (item #15). `closureAudit.service.ts`: novo tipo `RiscoReabertura` (`BAIXO|MÉDIO|ALTO|CRÍTICO` — reabertura→CRÍTICO; prematuro nota/CSAT ≤2→ALTO; prematuro→MÉDIO; resolução real→BAIXO) calculado centralmente em `auditarEncerramento`; `AuditoriaEncerramento` ganhou `riscoReabertura`, `semConfirmacao`, `motivoStatus`, `clienteId`, `clienteNome` (include client); `diagnosticarPorRegras`/IA propagam `semConfirmacao`. `resumoAuditoriaEncerramentos` estendido: `taxaEncerramentoCorreto`, `semConfirmacao`, `problemaNaoResolvido` (count por `motivoStatus='encerrado_sem_resolucao'` via constante), `notaMedia`, `porRisco`, `porTipo`, `porAnalista` (auditados/prematuros/resoluções/reaberturas/nota média), `porCliente`. Filtros `assigneeId` e `clienteId` em lote/resumo/lista. Exportação CSV `exportarAuditoriaCsv` (separador `;`, BOM) + rota `GET /helpdesk/closure-audit/exportar` (admin/gerente). Frontend `AuditoriaEncerramentoPage.tsx`: 8 cards (novos: Encerramento Correto %, Sem Confirmação, Sem Resolução, Nota Média, Risco Crítico), badges de risco na lista, seções "Distribuição por Risco", "por Tipo", tabelas "por Analista" e "por Cliente", modal com risco/confirmação/motivo status, botão Exportar CSV. TESTE #33 (3 novos: risco por tipo, resumo evoluído, CSV). | Backend **369/369** (34 arquivos; 8 em `closure-audit.test.ts`), tsc backend 20 pré-existentes (0 novos), frontend tsc 0, frontend 5/5. |
| 2026-08-15 | 12 | **FASE 12 — Fases B/C/D: Relatório Analítico consolidado** (relatórios com filtros combinados + comparação + exportação). Novo `relatorios.service.ts` genérico (`gerarRelatorioAnalitico`) com `buildWhere` combinando período/fila/canal/prioridade/status/etapa/departamento/analista/cliente/categoria/assunto (contains insensitive p/ SQLite); resumo com indicadores (tickets, resolvidos, tempo resposta/resolução, SLA, CSAT, FCR) + comparativo com período anterior de mesma duração; tendência diária; agrupamentos por canal/prioridade/status/categoria/departamento/fila/analista/cliente/assunto; views de tempo (Timetracking por tipo + `TicketDepartmentTime` por departamento), desenvolvimento (`horasDesenvolvimento` + OS `horasDev`) e implantação (OS implantação: total/concluídas/média horas dev/horas suporte). Exportação CSV (separador `;`) e PDF (pdfkit, Promise<Buffer> via evento `end`) + `obterOpcoesFiltros` (dropdowns). Rotas `GET /api/analytics/relatorios`, `/relatorios/csv`, `/relatorios/pdf`, `/relatorios/opcoes` (admin/gerente). Página `/app/relatorios/analitico` (`RelatorioAnalitico.tsx`) com filtros, atalhos de período, cards com delta pills, gráficos (linha/barras/pizza), tabelas e aba "Tempo/Dev/Implantação", botões CSV/PDF/Imprimir. Relatórios existentes intactos. | Backend **366/366** (6 testes em `relatorios.test.ts` — TESTE #32), tsc backend 20 pré-existentes (0 novos), frontend tsc 0, frontend 5/5. |
| 2026-08-15 | 12 | **FASE 12 — Ajustes Finais de Fluxo, Relatórios e Auditoria (Fase A — retorno do fora do horário)**. **CAUSA RAIZ do item #1**: `sla.scheduler.ts` `processarAguardandoExpediente` enviava mensagem hard-coded "Responda com o *nome* do departamento" (fluxo paralelo, sem reutilizar o fluxo canônico de boas-vindas) e não limpava o estado do bot. **Fix**: quando o ticket volta de `aguardando_expediente` sem departamento, o scheduler agora chama `enviarMenuInicial(ticket.id)` (mesmo mecanismo do início do atendimento — interpola `mensagemBoasVindas`, lista interativa `dept_<slug>` com fallback texto, marcador "[Bot] Menu de departamentos enviado"), limpa `botFluxo=null` no ticket e `limparEstadoConversa(phoneDigits)` (estado in-memory). Ramo com departamento preservado (vai à fila com mensagem "expediente iniciou"). `processarAguardandoExpediente` exportado para teste. Novo `expediente-return.test.ts` (TESTE #31): fora do horário → `aguardando_expediente` + msg de fora do horário SEM "selecione o departamento" → JOB retorno → etapa inicial (`etapaInicialSlug`) + `botFluxo=null` + stage event → marcador do menu → seleção de departamento → fila + `awaiting_description` → descrição → assunto + protocolo + fila → `abrirChamadoPorAtendente` → `em_atendimento`. | Backend **360/360** (1 novo), tsc 20 erros pré-existentes (0 novos). |
| 2026-08-15 | Bot | **Correção — Fluxo Bot Pós-Departamento (CAUSA RAIZ: fluxo não-determinístico após a escolha do setor)**. Após o cliente selecionar o departamento, o bot setava `IDLE` sem estado persistente e dependia de heurísticas frágeis (a pergunta de empresa só vinha depois da descrição e apenas se a IA rodasse; descrição curta/media não classificava assunto; empresa inexistente era criada automaticamente). **Fix**: novo campo `Ticket.botFluxo` (fonte de verdade no banco: `awaiting_company`/`awaiting_description`) — `schema.prisma` + `prisma db push`. Seção 7 do handler: após o departamento, se o contato tem empresa vinculada (Client por telefone) pede a descrição (`awaiting_description`); senão pergunta o nome da empresa (`awaiting_company`). Seção 9 determinística: `awaiting_company` busca no CRM, vincula ao confirmar (colaborador criado/atualizado por telefone), lista opções numeradas se múltiplas, e empresa não encontrada **NÃO cria vínculo** (re-pergunta); `awaiting_description` classifica assunto/categoria/prioridade com `analisarDescricaoProblema` (fallback local determinístico — `hasClaude()` false em teste), fallback `"Sem assunto"`, salva descrição em `observacoes`, confirmação + posição na fila, `botFluxo=null`. Fluxo legado (sem botFluxo) preservado intacto. Encerramento/confirmação/CSAT/menu NÃO alterados. **Refino (spec urgente)**: protocolo gerado na criação (`#TKT-YYYYMMDD-NNNN`, `generateProtocolo` com lock) e incluído na confirmação ao cliente ("Protocolo: #XXXXXX"); `abrirChamadoPorAtendente` reutiliza protocolo existente (removido o erro "Ticket ja triado"); timeline com `TicketStageEvent` para "Empresa identificada" e "Descrição recebida / assunto identificado"; logs estruturados `[BOT FLOW]`/`[TICKET]`. | Backend **359/359** (8 testes em `whatsapp-flow-pos-departamento.test.ts` cobrindo os testes 1-11: empresa existente/sem empresa, empresa não encontrada, múltiplas, departamento inválido + por nome, duplicidade de webhook, reinício, e TESTE CRÍTICO protocolo na confirmação), tsc 20 erros pré-existentes (0 novos). |
| 2026-08-14 | Menu | **Menu enxuto restaurado (estrutura definitiva)**: `navigation.ts` reescrito com a organização conceitual exata — 📊 Visão Geral (Dashboard), 🎫 Atendimento (Chamados, Atendimento ao Vivo, Quadro de Atendimento), 📈 Gestão (Gestão & Indicadores ▸ Métricas Operacionais/Métricas de Negócio/Dashboard Executivo; Auditoria IA ▸ Auditoria IA/Encerramento/por Analista; Relatórios ▸ Relatório Gerencial; Aprovações), 👥 Clientes (Clientes, Negócios, Ordens de Serviço), ⚙️ Operação (Tempo & Produtividade, Tarefas Internas, Base de Conhecimento, Automações), 🔗 Integrações (WhatsApp, Chatbots), ⚙️ Administração (Configurações). `NavItem.children` adicionado para submenus de nível 2 com accordion vertical e flyout no modo horizontal; `itemVisible` lida com role em submenus. `Layout.tsx` com suporte a subitens, auto-open de grupo+submenu pela rota ativa, rodapé preservado (`bottom-50`/`text-white/40` + "Codemed Hub v1.0"), Configurações duplicado do rodapé do menu removido (único acesso via grupo Administração). Nenhuma rota/página/API/componente removido — todas as rotas continuam acessíveis. | Frontend tsc 0, testes frontend 5/5. |
| 2026-08-14 | Bot | **Correção do fluxo de encerramento/avaliação (CAUSA RAIZ: listas interativas do Baileys via `relayMessage` retornam success sem entrega)**. 1) `whatsapp-message-service.enviarListaInterativa` agora envia **texto numerado SEMPRE** para Baileys/webjs (provider desconhecido = texto) e mantém lista interativa apenas para evolution/cloud — corrige avaliação e saudação que nunca chegavam. 2) Novo fluxo obrigatório pós-encerramento: `iniciarConfirmacaoResolucao` pergunta "*Seu problema foi resolvido?* (1 Sim / 2 Não)" ANTES da avaliação; SIM → `finalizarAtendimento` (CSAT); NÃO → pergunta descrição → `motivoStatus='encerrado_sem_resolucao'` + `resumoFinal` + notificação a analista/supervisores (`encerrado_sem_resolucao`) + `logAction` auditoria → avaliação. Novos estados `aguardando_confirmacao`/`aguardando_descricao` (constants). `buscarConfirmacaoPendente` + `processarRespostaEncerramento` interceptam resposta no handler canônico ANTES da criação de ticket (ZERO novo ticket; resposta inválida re-pergunta). `encerrarTicket(finalizarCsat)` e `moveTicketEtapa(concluido)` agora disparam confirmação em vez de CSAT direto. Scheduler `processarAgendamentosCsat` não envia CSAT automático para tickets em confirmação (inclui evaluationStatus nulo para legados). Testes: guard-rail do fluxo SIM/NÃO + resposta inválida + "mensagem com confirmação pendente NÃO cria novo ticket"; E2E atualizado com passo SIM. | Backend **351/351** (3 novos), tsc 20 erros pré-existentes (0 novos). Fix de determinismo de horário em `ticket-lifecycle-e2e` e `whatsapp-handler.smoke` (`abrirAtendimentoSempre`). |
| 2026-08-14 | 11 | Testes finais de regressão (FASE 11): suíte completa backend + frontend + typecheck executados em sequência — cobre todos os fluxos críticos (WhatsApp → ticket → atendimento → encerramento → avaliação, CSAT, aprovações, triagem, SLA, auditorias F7/F8, analytics). | Backend 348/348 (31 arquivos), frontend 5/5, tsc backend 22 erros pré-existentes (0 novos), tsc frontend 0. Roadmap v1.4 completo (Fases 0-11 ✅). |
| 2026-08-14 | 10 | Documentação final: `AGENTS.md` seção "Progresso Recente" reescrita com o Roadmap v1.4 (Fases 4-9) consolidado e atualizado; `docs/IMPLEMENTATION_PROGRESS.md` mantido como canônico de progresso (tabela + histórico com evidências). | Arquivos de documentação atualizados. |
| 2026-08-14 | 9 | Refatoração não-destrutiva: novo `whatsapp-utils.ts` (`normalizePhone`, `phoneDigitsFromChat`) deduplica o padrão `replace(/[^\d]/g,'')` (mesma regex, equivalente por construção) repetido em 19 pontos. Aplicado nos providers (cloud-api, evolution-api, baileys-provider, whatsapp-webjs), `whatsapp-message-service.ts` e `whatsapp.controller.ts`. **Handler canônico (`whatsapp-message-handler.ts`) e legado (`whatsapp.service.ts`) intactos** (regra AGENTS.md). Testes `whatsapp-utils.test.ts` (3) provam equivalência. | Backend 348/348 (3 novos), tsc 22 erros pré-existentes (0 novos). |
| 2026-08-14 | 8 | Auditoria individual por analista: novo `agentReport.service.ts` gera relatório por agente (`gerarRelatorioAnalista`) com resumo (tickets, resolvidos, taxa de resolução, tempo médio de resposta, FCR, nota média de auditoria via `aIAgentAudit`, CSAT médio, mensagens do agente) + lista de tickets auditados (nota/classificação por ticket, CSAT, tempo 1ª resposta, alertas). `getReplayConversa` retorna o replay completo do ticket (mensagens ordenadas com `fromMe`, `content`, `createdAt` + auditoria por mensagem: nota, alertas, pontos fortes/melhoria, sugestão). Rotas: `GET /helpdesk/audit/agent/:id` e `GET /helpdesk/audit/agent/:id/ticket/:ticketId` (admin/gerente). Página `/app/helpdesk/auditoria-analista` (`AuditoriaAnalistaPage.tsx`) com seletor de analista/período, 7 cards de métricas, lista de tickets com botão Replay (modal de conversa estilo chat com auditoria inline). Menu lateral + rota. Testes `agent-report.test.ts` (5). | Backend 345/345 (5 novos), frontend 5/5, tsc 0 erros novos. |
| 2026-08-14 | 7 | Auditoria de Encerramento (evolução do monitor IA): novo `closureAudit.service.ts` classifica tickets encerrados como `encerramento_prematuro` / `resolucao_real` / `reabertura` por regras locais (confirmação do cliente, CSAT, insatisfação) + análise Claude quando disponível. Detecta reabertura cruzando novos tickets no mesmo telefone após `dataFechamento` (exclui o próprio ticket — fix de corrida createdAt>dataFechamento). Rotas: `GET /helpdesk/closure-audit/:id`, `/encerrados`, `/lote`, `/resumo`. Página `/app/helpdesk/auditoria-encerramento` (`AuditoriaEncerramentoPage.tsx`) com cards de resumo (auditados, prematuros, reais, reaberturas + taxas), lista de encerrados, auditoria individual (nota 0-10, diagnóstico, evidências, CSAT, recomenda reabertura) e auditoria em lote. Menu lateral + rota. Testes `closure-audit.test.ts` (5). | Backend 340/340 (5 novos), frontend 5/5, tsc 0 erros novos. |
| 2026-08-14 | 6 | Dashboard Executivo consolidado: novo `GET /api/analytics/executivo?dias=7|30|90` (`dashboardExecutivo.service.ts`) que retorna em UMA chamada resumo (tickets, resolvidos, tempo médio resposta/resolução, SLA, CSAT, FCR), tendência diária, tickets por status/dia, por etapa, canal, categoria, departamento, agente (com tempo médio + CSAT) e tempo médio por fila — tudo com período parametrizável (endpoints antigos fixavam "mês") + comparativo com período anterior. Página `/app/relatorios/executivo` (`DashboardExecutivo.tsx`) com cards + delta pills, área de tendência, barras empilhadas por status, pizzas por etapa/canal, linha CSAT, barras por fila, tabelas por departamento/agente/categoria. Menu lateral + rota. Testes `dashboard-executivo.test.ts` (3). | Backend 335/335 (3 novos), frontend 5/5, tsc 0 erros novos. |
| 2026-08-14 | 5 | Relatório Gerencial consolidado: página `/app/relatorios/gerencial` consumindo `GET /api/analytics/relatorio-semanal` (já existente) com cards de resumo (tickets, resolvidos, tempo médio resposta/resolução, SLA, CSAT, FCR), comparativo com período anterior (delta pills), gráficos recharts (tendência por dia, pizza por canal, barras por categoria), performance por agente, top clientes/departamentos/tickets-problema, sugestões IA, prévia da mensagem WhatsApp (copiar) e envio via `POST /api/analytics/relatorio-semanal/enviar`. Menu lateral + rota adicionados. Testes `weekly-report.test.ts` (3) cobrem geração do relatório, contabilização de ticket no período e formatação da mensagem WhatsApp. | Backend 332/332 (3 novos), frontend 5/5, tsc 0 erros novos. |
| 2026-08-14 | 4 | Aprovações via WhatsApp: `Aprovacao` ganhou `canal` (default interno), `telefoneAprovador`, `token` (@unique, prefixo `aprov_`, TTL 48h) e `expiraEm`. `solicitarAprovacao` aceita `canal`/`telefoneAprovador` e dispara WhatsApp para aprovadores com telefone cadastrado; `enviarAprovacaoWhatsApp` envia lista interativa (`aprovacao_<id>_aprovar`/`rejeitar`) + link fallback `/aprovacoes/<token>`; `decidirAprovacaoPorToken` (sem auth, sem criar ticket); `processarRespostaAprovacaoWhatsApp` intercepta resposta no handler canônico ANTES da criação de ticket (ZERO novo ticket) via interactiveId ou texto (aprovar/rejeitar/1/2/sim/não) casado pelo telefone do aprovador. Frontend: botão "WhatsApp" na lista de aprovações, página pública `/aprovacoes/:token` e rotas `POST /api/aprovacoes/:id/enviar-whatsapp` e `POST /api/aprovacoes/token/:token/decidir`. | Backend 329/329 (5 novos), frontend 5/5, tsc 0 erros novos. |
| 2026-08-14 | 3 | Time Tracking integrado ao ticket: TimeEntry ganhou `clienteId`, `tarefaId`, `setorId`; auto-start de timer ao criar tarefa no ticket (`criar-kanban`); relatório de consumo por cliente (Atendimento/Desenvolvimento/Implantação/Outro/Total) em `GET /api/timetracking/consumption/client`; blocos de tempo em `GET /api/timetracking/ticket/:id/blocks` e exibidos na timeline do ticket; aba "Consumo por Cliente" no TimeTrackingPage. | Backend 324/324 (4 novos), frontend 5/5, tsc 0 erros novos. |
| 2026-08-14 | 2 | Fluxos críticos validados. Suíte estabilizada (arquivos de teste em sequência — elimina corrida de SQLite compartilhado). Teste frontend `auth.test.tsx` atualizado (tokens migraram para cookies). Fix: tickets `aguardando_expediente` agora visíveis para agentes no kanban (`getKanban` — filtro OR). | Backend 320/320 estável (2 execuções consecutivas); frontend 5/5; tsc 0 erros novos. Commit `d8394b9` (checkpoint FASE 0-2). |
| 2026-08-13 | 1 | Infra de testes padrão: `helpers/test-utils.ts` (cleanup, ambiente helpdesk, horário 24/7, sendMessage mock) + smoke do handler canônico `whatsapp-handler.smoke.test.ts` (4 testes) | 318/318 testes passando; tsc 0 erros novos; cobertura do caminho mensagem→ticket→departamento→fila |
| 2026-08-13 | 0 | Diagnóstico do sistema; levantamento de módulos, schema, rotas, fluxos | Backlog mapeado — ver DIAGNÓSTICO |
| 2026-07-10 | — | Correções críticas: encerramento pós-avaliação + lista interativa mascarada | 314/314 testes passando; docs/critical-business-rules/ticket-lifecycle.md criado |

## Regras operacionais

1. **NÃO DESTRUIR**: antes de remover/alterar qualquer fluxo, verificar dependências (grep de chamadores) e rodar testes de regressão.
2. **Trabalho pendente NÃO é trabalho feito**: uma fase só é `✅` após implementação + testes + documentação.
3. **Preservar fluxos críticos** documentados em `docs/FLOWS.md` e protegidos por testes em `backend/src/__tests__/ticket-closure-regression.test.ts` e `interactive-message-flow.test.ts`.
4. Atualizar este arquivo ao final de cada fase com evidências (testes passando, tsc limpo, arquivos tocados).

## Mapa de prioridades abertas (detalhado)

- [ ] Ordem de serviço (Orders) — refinar fluxo de implantação/signature (parcialmente pronto)
- [ ] Dark mode global
- [ ] Auditoria IA: detectar encerramento prematuro / resolução real / reabertura (base: `aiAgentMonitor.service.ts` + `ai-audit.service.ts`)
- [ ] Dashboard IA: cards diário/semanal/por analista (base: `weeklyReport.service.ts`, `metrics.service.ts`, `AuditoriaAtendimento.tsx`)
- [ ] E2E WhatsApp real (Baileys v6.7.23) — pendente de conexão ativa no ambiente

## FASE 12 — Ajustes Finais de Fluxo, Relatórios e Auditoria (checklist da spec #36)

- [x] **Fora do horário → BOAS-VINDAS**: retorno de `aguardando_expediente` reutiliza o fluxo canônico (`enviarMenuInicial`) + limpeza de estado (`botFluxo=null` + `limparEstadoConversa`). TESTE #31 (`expediente-return.test.ts`).
- [x] **Validar seleção de departamento** no retorno do expediente (menu → `dept_<slug>` → fila + `awaiting_description`) — coberto pelo TESTE #31.
- [x] **Validar entrada na fila** após retorno do expediente (etapa inicial + stage event + protocolo) — coberto pelo TESTE #31.
- [x] Fase B — Relatórios: **novo módulo `relatorios.service.ts`** (`GET /api/analytics/relatorios`) com filtros combinados (período, fila, canal, prioridade, status, etapa, departamento, analista, cliente, categoria, assunto `contains` insensitive) + atalhos de período (7/14/30/90/personalizado no frontend) + cards de indicadores com delta pills + comparação com período anterior (tickets, fechados, tempo resposta/resolução, CSAT, SLA, FCR). `obterOpcoesFiltros` para dropdowns (`GET /relatorios/opcoes`). Página `/app/relatorios/analitico` (`RelatorioAnalitico.tsx`) + menu Relatórios ▸ Relatório Analítico.
- [x] Fase D — Exportação de relatórios: `GET /relatorios/csv` (separador `;`, seções por indicador/agrupamento) e `GET /relatorios/pdf` (pdfkit, cards + tabelas, multi-página) + botão Imprimir (`window.print`) na página.
- [x] Fase C — Views por analista, cliente, categoria, assunto, fila, departamento, **tempo** (Timetracking por tipo — dev/suporte/implantação/etc — e por departamento via `TicketDepartmentTime`), **desenvolvimento** (`horasDesenvolvimento` ticket + `horasDev` OS) e **implantação** (OS `tipoImplantacao='implantacao'`, concluídas, média horas dev, horas suporte). Aba "Tempo / Dev / Implantação" na página.
- [x] Fase E — Auditoria de encerramento evoluída: risco de reabertura (BAIXO/MÉDIO/ALTO/CRÍTICO), indicadores de qualidade, auditoria por analista/cliente, gráficos, drill-down e exportação.
- [x] Fase F — Segurança (validação no backend), performance (agregações no banco), fonte única de dados, TESTES #33–#34 (auditoria) e regressão completa (testes existentes intactos — não quebrar o que funciona).
